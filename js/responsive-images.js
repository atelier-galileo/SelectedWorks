/* responsive-images.js — placeholder, srcset disabled for now */
function addSrcset(img, src) {}
function getMobileSrc(src) { return src; }
function getThumbSrc(src) { return src; }
function getDesktopSrc(src) { return src; }
